apiVersion: v1
kind: Service
metadata:
  name: antifraud-api
spec:
  type: LoadBalancer
  selector:
    app: antifraud-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
